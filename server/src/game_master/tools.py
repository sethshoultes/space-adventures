"""Game Master Tools - Functions the AI agent can call.

These tools let the Game Master manipulate game state,
check player inventory, resolve combat, etc.
"""

# TODO: Define tool functions for the agent
# - check_inventory(player_id) -> list items
# - modify_ship_system(system, action) -> result
# - resolve_skill_check(skill, difficulty) -> success/fail
# - award_xp(amount) -> new total
# - give_item(item_id) -> inventory update
# - update_relationship(npc, delta) -> new value
# - trigger_event(event_id) -> narrative text
